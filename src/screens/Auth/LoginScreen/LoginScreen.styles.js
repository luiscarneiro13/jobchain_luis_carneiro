import { StyleSheet } from "react-native"

export const styles = new StyleSheet.create({
    info: {
        color: "#333",
        marginVertical: 15,
        opacity: 0.6,
        textAlign: "center"
    }
})