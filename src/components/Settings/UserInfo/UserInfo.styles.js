import { StyleSheet } from "react-native"

export const styles = new StyleSheet.create({
    content: {
        alignItems: "center"
    },
    avatar: {
        marginTop: 40
    },
    identity: {
        color: "#333",
        fontSize: 26,
        // fontWeight: "bold",
        marginTop: 20
    },
    email: {
        opacity: 0.6
    }
})