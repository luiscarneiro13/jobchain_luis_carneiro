import { StyleSheet } from "react-native"

export const styles = new StyleSheet.create({
    container: {
        backgroundColor: "#F3F0EA",
        paddingTop: 20,
        marginBottom: 100,
    },
    content: {
        paddingBottom: 150
    },
    
})